{
  "name": "setup-lazarus",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild source/main.js --bundle --platform=node --format=cjs --target=node24 --outfile=dist/index/index.js && esbuild source/post.js --bundle --platform=node --format=cjs --target=node24 --outfile=dist/post/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ollydev/setup-lazarus.git"
  },
  "author": "ollydev",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ollydev/setup-lazarus/issues"
  },
  "homepage": "https://github.com/ollydev/setup-lazarus#readme",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/cache": "^6.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/tool-cache": "^4.0.0",
    "base-64": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}